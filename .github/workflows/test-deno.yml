name: "Test Deno"

on:
  workflow_dispatch:

jobs:
  test:
    name: "Test Deno"
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: "Install Deno"
        run: deno install

      - name: "Run Sample"
        run: deno run --allow-read scripts/sample.deno.js
